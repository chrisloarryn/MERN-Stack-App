import React from 'react'

// Components

// CSS Styles
import './Map.css'

const Map = () => {
    return (
        <div className={`map ${props.className}`} style={props.style} >
            
        </div>
    )
}

export default Map
